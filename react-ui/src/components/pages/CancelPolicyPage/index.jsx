import React from 'react';
import BaseTemplate from 'components/templates/BaseTemplate';
import CancelPolicy from 'components/LV3/CancelPolicy';

const CancelPolicyPage = React.memo(() => (
  <BaseTemplate>
    <CancelPolicy />
  </BaseTemplate>
));

export default CancelPolicyPage;
